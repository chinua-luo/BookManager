# BookManager Development Rules

1. 每次改动完成并通过验证后，都必须创建一个只对应本次改动的 Git commit，以便后续追踪、比较和回滚。不得把互不相关的修改混入同一个 commit。
2. 每次代码改动后，都必须编写或更新与改动相关的测试。向用户交付前，必须运行全部自动化测试以及适用于本次改动的构建、语法检查和其他验证；只有在所有测试和验证全部通过后才能交付。
